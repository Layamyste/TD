#EXERCICE 1

import random

def bool_aleatoire() -> bool:
    """
    Renvoie True ou False aléatoirement
    """
    #Compléter cette ligne

#EXERCICE 2 

while True:
    print("{}, entre ton mot".format(joueur.nom)) 
    mot = input()  
    if mot == "":  
    #mot manquant ici ->
    ("Ce n'est pas un mot")  
    elif self.mots.mot_existant(mot):  
        print("Ce mot a déjà été dit")  
    #mot manquant ici ->
    :
        self.mots.ajouter_mot(joueur.uuid, mot)  
        break 

#EXERCICE 3

#mot manquant ici ->
      True:
    message = "Choisis quelqu'un à éliminer parmi :\n"
    #mot manquant ici ->
     i in range(len(self.joueurs)): 
        message += "{} - {}\n".format(i + 1, self.joueurs[i].nom) 
    print(message)  
    vote = input()  
    try:
        vote = int(vote) 
        assert len(self.joueurs) >= vote >= 1  
        vote -= 1  
        break  
    except ValueError:  
        print("Entrez un nombre valide.")
    except AssertionError:
        #mot manquant ici ->
        ("Ce joueur n'existe pas")



